# catong_gen 端到端测试流程文档

> 适用版本：`catong_gen` Phase 0~8（FastAPI 后端 + SQLite + 拼装引擎 + 多供应商适配层）。
> 测试目标：通过 Swagger 手动验证 / 通过 pytest 自动验证「**小说 → 角色 → 项目分镜 → 图片提示词 → 出图 → 导出**」全链路可跑通。
>
> 设计文档参考：[`设计文档/01-项目总览与技术选型.md`](./01-项目总览与技术选型.md) ~ [`09-环境与部署.md`](./09-环境与部署.md)

---

## 目录

- [1. 测试环境准备](#1-测试环境准备)
- [2. 模型与供应商配置](#2-模型与供应商配置)
- [3. 启动后端 & 进入 Swagger](#3-启动后端--进入-swagger)
- [4. 端到端测试流程（按业务顺序）](#4-端到端测试流程按业务顺序)
  - [4.1 前置健康检查](#41-前置健康检查)
  - [4.2 小说中心](#42-小说中心-phase-2)
  - [4.3 角色中心](#43-角色中心-phase-3)
  - [4.4 项目与分镜](#44-项目与分镜-phase-4)
  - [4.5 画风库（可选）](#45-画风库可选-phase-8)
  - [4.6 图片提示词推导](#46-图片提示词推导-phase-5)
  - [4.7 图片生成](#47-图片生成-phase-6)
  - [4.8 导出条漫](#48-导出条漫-phase-7)
- [5. 最小闭环（6 个接口）](#5-最小闭环6-个接口)
- [6. 常见错误与排查](#6-常见错误与排查)
- [7. 自动化测试（pytest）](#7-自动化测试pytest)
- [8. 接口速查表](#8-接口速查表)

---

## 1. 测试环境准备

### 1.1 系统依赖

| 项 | 版本 / 说明 |
|---|---|
| OS | Windows 10/11（开发机）；Linux/macOS 同样可跑 |
| Python | **3.11**（conda env `catong_gen`） |
| 数据库 | SQLite（WAL，自动创建于 `data/catong_gen.db`） |
| 浏览器 | 现代浏览器（用于 Swagger UI） |
| 网络 | 必须能访问 LLM / 生图供应商 API（公网或自建网关） |

### 1.2 初始化（首次或重置环境时执行）

打开 **Anaconda Prompt**（普通 `cmd` / `PowerShell` 不支持 `conda activate`）：

```bat
:: 1) 创建并激活 conda 环境
conda create -n catong_gen python=3.11 -y
conda activate catong_gen

:: 2) 安装后端依赖
cd /d D:\github.io\catong_gen
pip install -r backend\requirements.txt

:: 3) 注入种子数据（13 提示词 + 3 片段 + 8 模板 + 8 预设 + 默认 settings）
cd backend
python -m app.seeds.seed
```

> **可选**：重置数据库——删除 `data/catong_gen.db*` 后再 `python -m app.seeds.seed`。
> **可选**：`backend/.pytest_cache` / `backend/test_phase5.db` 是测试副产物，可随时删除。

### 1.3 一键脚本（项目内已提供）

| 脚本 | 作用 |
|---|---|
| `scripts/seed.bat` | 双击即跑 seed（自动 `conda activate catong_gen`） |
| `scripts/dev.bat` | 双击即启动开发服务器（端口 8300） |
| `scripts/run_e2e_test.bat` | **本流程文档新增** —— 一键启动后端 + 跑端到端测试 |

---

## 2. 模型与供应商配置

`catong_gen` 把所有外部能力都抽象成「**LLM 文本供应商**」和「**图片生成供应商**」两类账号，存到 `provider_accounts` 表（也兼容 `settings` 表的 K/V 形式回退）。每个调用方不写死 API。

### 2.1 LLM 供应商（用于：角色推导 / 分镜拆分 / 项目摘要 / 图片提示词推导）

`app/adapters/llm.py` 内置了三套 OpenAI 兼容端点：

| `provider_code` | 用途 | 默认 `base_url` | 默认 `model` | 何时使用 |
|---|---|---|---|---|
| `deepseek` | **推荐默认**（中文长文本/角色/分镜） | `https://api.deepseek.com/v1` | `deepseek-chat` | 长文本摘要、角色 JSON 解析、分镜拆分 |
| `grsai` | GRSai 转发（也可指向其他兼容服务） | `https://api.grsai.com/v1` | `deepseek-chat` | 与 `deepseek` 互为兜底 |
| `volcengine` | 火山方舟（豆包） | `https://ark.cn-beijing.volces.com/api/v3` | `doubao-pro-32k` | 字节生态接入 |

#### 2.1.1 两种配置方式（二选一）

**方式 A：走 `provider_accounts` 表（推荐）**

通过 Swagger 写入：

```http
POST /api/provider-accounts
{
  "provider_code": "deepseek",
  "base_url": "https://api.deepseek.com/v1",
  "api_key": "sk-xxxxxxxxxxxxxxxxxxxxxxxx",
  "model": "deepseek-chat",
  "remark": "我的 DeepSeek 账号"
}
```

> 也可以一次性写多账号；`valid=true` 的同 `provider_code` 第一条会被使用。

**方式 B：写 `settings` 表（K/V 兜底）**

只有 seed 阶段从 `_inspect/settings.json` 自动迁入时才会用，**手动配置请走方式 A**：

```http
PUT /api/settings
{ "key": "deepseek_api_key", "value": "sk-xxxxxxxx" }
```

#### 2.1.2 LLM 模型选型建议

| 任务 | 推荐模型 | 备选 |
|---|---|---|
| 项目摘要（≤200 字） | `deepseek-chat` | `doubao-pro-32k` |
| 角色推导（输出 JSON 数组） | `deepseek-chat` | `grsai/deepseek-chat` |
| 分镜拆分（短句版，输出 1./2. 列表） | `deepseek-chat` | `grsai/deepseek-chat` |
| 图片提示词推导（三段式长 prompt） | `deepseek-chat`（max_tokens 建议 4096+） | `doubao-pro-32k` |

> **重要约束**：`/api/roles/derive` 输出的 `roles[*].name` / `content` 会被正则/JSON 三级容错解析，模型至少要能稳定吐 JSON；推荐 `deepseek-chat`。

#### 2.1.3 设置默认供应商

```http
GET /api/settings      -> 查 llm_default_provider 当前值
PUT /api/settings
{ "key": "llm_default_provider", "value": "deepseek" }
```

调用 `derive_roles / do_split / generate_summary / derive_script_prompt` 时**不传** `provider_code` 就走这个默认值。

### 2.2 图片生成供应商（用于：Phase 6 出图）

后端走 `OpenAI 兼容 /images/generations` 协议（见 `app/adapters/image_provider.py`）。当前实现只对接了**一个账号位**（`grsai`），但**任何** OpenAI Images 兼容端点都可以填进去。

#### 2.2.1 配置示例

```http
POST /api/provider-accounts
{
  "provider_code": "grsai",
  "base_url": "https://api.grsai.com/v1",
  "api_key": "grsai-xxxxxxxxxxxxxxxxxxxx",
  "model": "nano-banana-2",
  "remark": "GRSai 香蕉 2 生图"
}
```

#### 2.2.2 模型 & 规格

| 字段 | 推荐值 | 备注 |
|---|---|---|
| `model` | `nano-banana-2` | 默认；GRSai 当前接入的文生图模型 |
| `aspect_ratio` | `3:4`（竖版条漫）/ `1:1` / `16:9` | seed 默认 `3:4` |
| `resolution` | `2K` / `4K` | seed 默认 `2K` |

> 切换供应商：在 `ImageGenerationService.generate_single()` 调用前**手动**改 `provider_code` 参数（Swagger 请求体里直接传）。底层是同一适配器类，**只要你的端点协议是 OpenAI Images 兼容**，就能跑。

#### 2.2.3 返回格式约束

适配器逻辑（见 `image_provider.py:88-111`）：

- 若返回 `{"data":[{"b64_json":"..."}]}` → 自动落盘到 `Output/<project_id>/1/image/image_<ms>.png`
- 若返回 `{"data":[{"url":"..."}]}` → 直接拿 URL（不会落盘）
- 其它形状 → 抛 `ValueError("Invalid response format from image provider")`

### 2.3 最小配置清单（够跑完流程）

| 必需 | 项 | 说明 |
|---|---|---|
| ✅ | `provider_accounts.deepseek` | 角色 / 分镜 / 摘要 / 图片提示词 都靠它 |
| ✅ | `provider_accounts.grsai`（或任一 OpenAI Images 兼容端点） | 出图 |
| 可选 | `provider_accounts.volcengine` | 火山方舟备选 |
| 可选 | `settings.llm_default_provider` | 不写默认 `deepseek` |
| 可选 | `settings.image_provider / image_model / image_aspect_ratio / image_resolution` | seed 已注入默认 |

> **没有任何 Key 也能跑测试** —— 自动化测试的「纯 LLM 步骤」会 **mock LLM/Adapter**，保证 CI / 离线环境可全绿；只有手动跑 Swagger 才需要真实 Key。

---

## 3. 启动后端 & 进入 Swagger

```bat
conda activate catong_gen
cd /d D:\github.io\catong_gen\backend
uvicorn app.main:app --reload --port 8300
```

启动成功后：

| 入口 | URL |
|---|---|
| **Swagger UI（推荐）** | <http://127.0.0.1:8300/docs> |
| ReDoc | <http://127.0.0.1:8300/redoc> |
| 健康检查 | <http://127.0.0.1:8300/api/health> |
| OpenAPI JSON | <http://127.0.0.1:8300/openapi.json> |

> Swagger 顶部的 tag 分组依次是：`health / prompts / assemble / novels / roles / settings / projects / image-derive / image-generation / export / art-styles / category-tags / prompt-*`。

---

## 4. 端到端测试流程（按业务顺序）

> 约定：所有 `id` 都在调用后**记下来**给下一步用。变量名（`NOVEL_ID` 等）只是文档占位符，请替换为实际返回值。

### 4.0 前置健康检查

| # | Method+Path | Body / Query | 期望 |
|---|---|---|---|
| 4.0-1 | `GET /api/health` | — | `{"status":"ok","app":"catong_gen",...}` |
| 4.0-2 | `GET /api/prompt-presets` | — | 返回 8 条预设（`role_derive_vip / storyboard_short_v1 / storyboard_long_v1 / dialogue_split_v1 / rewrite_v1 / summary_v1 / plot_manga_fusion_bw / portrait_s1_v1`） |
| 4.0-3 | `GET /api/assemble/stages` | — | 返回 8 个阶段 |
| 4.0-4 | `GET /api/provider-accounts` | — | 至少能看到 `deepseek` 账号（手动配的或 seed 迁入的） |

### 4.1 小说中心（Phase 2）

#### 4.1.1 导入小说（任选其一）

**A. 文本直传（Swagger Try it out）**

```http
POST /api/novels
{
  "name": "测试小说-霜降",
  "text": "霜降\n\n第一章 山中夜雨\n\n雨水顺着青瓦滴下来，林七蹲在檐下，眼神像被火烤过……（≥300 字）"
}
```

**B. 上传 txt 文件**

```http
POST /api/novels/import-txt    Content-Type: multipart/form-data
file=@D:\github.io\catong_gen\_inspect\sample_novel.txt
```

> 响应里 `id` 字段 → 记为 **`NOVEL_ID`**。

#### 4.1.2 预览清洗 diff

```http
POST /api/novels/clean
Body (query novel_id + json body):
  novel_id = NOVEL_ID
  { "rules": ["format", "serial", "punct", "speech_quote"], "apply": false }
```

期望：返回 `applied=false`、`counts` 各项 ≥ 0、`diff` 是 unified diff 文本。

#### 4.1.3 真正应用清洗

同上 body 改 `"apply": true`。返回 `is_format_cleaned / is_serial_cleaned / is_punct_cleaned` 三个布尔标记。

#### 4.1.4 校验

```http
GET /api/novels/detail?novel_id=NOVEL_ID
```

应看到 `content` 已被改写、三个 `is_*_cleaned=true`、`original_text` 保留原文。

### 4.2 角色中心（Phase 3，需 LLM）

#### 4.2.1 AI 推导角色

```http
POST /api/roles/derive
{
  "novel_id": NOVEL_ID,
  "preset_id": "role_derive_vip",
  "replace_existing": true
}
```

- 不传 `provider_code` 走 `settings.llm_default_provider`。
- 响应里 `count` ≥ 1，`log_id` 可去 `GET /api/render-logs/detail?log_id=…` 查看。
- 失败码：缺少 Key → 409；LLM 解析失败 → 502；小说不存在 → 404。

#### 4.2.2 列出 & 挑选

```http
GET /api/roles?novel_id=NOVEL_ID
```

挑 1~2 个角色，记 **`ROLE_ID_1` / `ROLE_ID_2`**。

#### 4.2.3 （可选）打标签

```http
PUT /api/roles/tags?role_id=ROLE_ID_1
Body: [
  {"category":"类型","value":"角色"},
  {"category":"时空","value":"古代"}
]
```

### 4.3 项目与分镜（Phase 4，需 LLM）

#### 4.3.1 创建项目

```http
POST /api/projects
{
  "name": "霜降-条漫版",
  "novel_id": NOVEL_ID,
  "mode": "manga",
  "derive_preset_id": "plot_manga_fusion_bw"
}
```

响应 `id` → **`PROJECT_ID`**。

#### 4.3.2 项目摘要

```http
POST /api/projects/summary?project_id=PROJECT_ID
{ "max_words": 200 }
```

#### 4.3.3 拆解分镜

```http
POST /api/projects/split?project_id=PROJECT_ID
{ "preset_id": "storyboard_short_v1", "replace_existing": true }
```

> `storyboard_short_v1` 在 seed 里默认 `is_active=true`，可省略 `preset_id`。
> 长句版 `storyboard_long_v1` 默认 `is_active=false`，想用需显式传。

#### 4.3.4 列出分镜

```http
GET /api/projects/scripts?project_id=PROJECT_ID
```

记下 `items[0..N-1]` 的 `id` 列表 → **`SCRIPT_IDS = [s1,s2,s3,...]`**。

#### 4.3.5 （可选）人工调整 & 重排

```http
PUT /api/projects/scripts/detail?script_id=s1
{ "content": "我手动改写的镜头描述……" }

PUT /api/projects/scripts/reorder?project_id=PROJECT_ID
{ "order": [s3, s1, s2] }
```

> 手工改 `image_prompt` 会自动置 `prompt_touched=true`，批量推导会跳过该镜；如要强制重算，先清空 `image_prompt`。

### 4.4 画风库（可选，Phase 8）

#### 4.4.1 新建画风

```http
POST /api/art-styles
{
  "code": "h2_default",
  "name": "韩漫默认",
  "category": "manga",
  "prefix": "顶级韩漫风格，融合精致日系2D插画美学，",
  "suffix": "，电影级光影，高对比度，",
  "negative_prompt": "禁止：水印、低分辨率、变形肢体、模糊",
  "recommended_model": "nano-banana-2",
  "recommended_aspect_ratio": "3:4",
  "recommended_resolution": "2K"
}
```

响应 `id` → **`STYLE_ID`**。

#### 4.4.2 绑定到项目

```http
POST /api/art-styles/bind
{ "project_id": PROJECT_ID, "style_id": STYLE_ID }
```

之后 `image-derive` 会自动在 prompt 里加 `prefix` + `suffix`。

> 跳过本步也能跑 —— `plot_manga_fusion_bw` 自带 `style_prefix` 片段。

### 4.5 图片提示词推导（Phase 5，需 LLM）

#### 4.5.1 单镜

```http
POST /api/image-derive/scripts/{s1}
Body: {}    # 用默认 preset + 默认 provider
```

响应：`{ script_id, image_prompt, render_log_id }`。

#### 4.5.2 批量（推荐）

```http
POST /api/image-derive/projects/{PROJECT_ID}/batch
Body: {}
```

响应：`{ total, success, failed, results:[...] }`。**所有分镜的 `script.image_prompt` 会被回填。**

#### 4.5.3 校验

```http
GET /api/projects/scripts?project_id=PROJECT_ID
```

每个分镜的 `image_prompt` 字段应当非空；`prompt_touched=true` 的分镜会被跳过。

#### 4.5.4 调试：拼装引擎独立 dry-run

```http
POST /api/assemble/preview
{
  "stage": "image_derive",
  "preset_id": "plot_manga_fusion_bw",
  "variables": {
    "shot_content": "测试镜头",
    "style_prefix": "韩漫画风，"
  },
  "persist_log": false
}
```

返回 `segments[]`（system / instruction / context / negative 切分）+ `rendered` 全文。**不调 LLM**，纯本地调试。

### 4.6 图片生成（Phase 6，需生图供应商）

#### 4.6.1 单镜生图

```http
POST /api/image-generation/generate
{
  "script_id": s1,
  "provider_code": "grsai",
  "model": "nano-banana-2",
  "aspect_ratio": "3:4",
  "resolution": "2K"
}
```

**同步返回** `{ task_id, status, image_url }`。第一张候选图**自动**被设为主图。记 **`TASK_ID`**。

#### 4.6.2 批量生图

```http
POST /api/image-generation/batch
{
  "script_ids": [s1, s2, s3],
  "provider_code": "grsai",
  "model": "nano-banana-2",
  "aspect_ratio": "3:4",
  "resolution": "2K"
}
```

返回 `{ total, success, failed, results }`。当前实现是**同步串行**（`services/image_generation.py:127-170`），3 张图预计 30s~数分钟。

#### 4.6.3 选主图

```http
POST /api/image-generation/select-main
{ "script_id": s1, "task_id": TASK_ID_2 }
```

把第 2 张候选图设为主图，写回 `script.main_image` 与 `script.selected_candidate`。

#### 4.6.4 查任务 & 候选

```http
GET /api/image-generation/tasks/{TASK_ID}
GET /api/image-generation/candidates/{s1}
GET /api/image-generation/tasks?script_id=s1
```

### 4.7 导出条漫（Phase 7）

#### 4.7.1 预检

```http
GET /api/export/info/{PROJECT_ID}
```

期望 `can_export=true`（至少 1 个分镜有 `main_image`）。否则先回到 §4.6 生图。

#### 4.7.2 创建并执行导出

```http
POST /api/export/create
{
  "project_id": PROJECT_ID,
  "export_style": "vertical",
  "page_numbers": true,
  "titles": false,
  "quality": 90
}
```

`export_style`：`vertical`（竖条长图）/ `horizontal`（横版）。

#### 4.7.3 下载长图

```http
GET /api/export/download/{EXPORT_TASK_ID}
```

浏览器会直接弹出下载（`export_<id>.jpg`）。实际文件在 `Output/<path>`。

#### 4.7.4 ZIP 打包

```http
POST /api/export/zip/{PROJECT_ID}
```

返回 `application/zip` 流。

---

## 5. 最小闭环（6 个接口）

> 没时间跑全流程？只跑这 6 步也能验通「小说 → 角色 → 项目分镜 → 图片提示词 → 出图 → 导出」。

| 步骤 | 接口 | 备注 |
|---|---|---|
| 1 | `POST /api/novels` | 灌一段 ≥300 字小说 |
| 2 | `POST /api/roles/derive?novel_id=…` | 推角色 |
| 3 | `POST /api/projects` → `POST /api/projects/split?project_id=…` | 建项目并拆镜 |
| 4 | `POST /api/image-derive/projects/{PROJECT_ID}/batch` | 推图片 prompt |
| 5 | `POST /api/image-generation/generate` | 出 1 张图 |
| 6 | `POST /api/export/create` + `GET /api/export/download/{task_id}` | 导出并下载长图 |

---

## 6. 常见错误与排查

| 错误码 | 触发场景 | 排查 |
|---|---|---|
| **409 LLMNotConfigured** | 没配 LLM Key 就调 `derive/split/derive-script` | `GET /api/provider-accounts` 看是否为空；空就 `POST /api/provider-accounts` 加一个 `deepseek` |
| **502 LLMError** | Key 错、余额不足、网络断 | 看响应 detail；可手动用同 Key 在 `https://api.deepseek.com` 试一次 |
| **404 / 422** `ValueError("Script not found")` 等 | 用错 id | 重新 `GET` 列表拿真实 id |
| **导出报 500** `Export failed` | 项目无分镜主图 | 跑 §4.6 至少给一个分镜选主图 |
| **图片返回 `Invalid response format`** | 生图供应商返回结构非 OpenAI 兼容 | 切换到 OpenAI Images 兼容端点 |
| **derive 没结果** | LLM 返回非 JSON 且无 `name: 描述` 行 | 换 `deepseek`；或换一段叙述更清晰的小说 |
| **同小说重复跑 derive 没生效** | `replace_existing=false` 默认 | 显式传 `"replace_existing": true` |
| **数据库锁** | 多 uvicorn 实例写同一个 `catong_gen.db` | 保持单实例；或换 `CATONG_DB_URL` 到 Postgres |

---

## 7. 自动化测试（pytest）

### 7.1 文件清单

| 文件 | 覆盖范围 | 是否调外部 API |
|---|---|---|
| `backend/tests/test_e2e_api.py` | 整条 API 链路在 `httpx.AsyncClient(ASGITransport)` 上跑 | **否**（LLM/Image 均 mock） |
| `backend/tests/test_e2e_full_flow.py` | 真实 `TestClient` 跑 Phase 0~8 端到端 80+ 用例 | **否**（同样 mock 外部） |
| `backend/tests/test_prompt_engine.py` | 拼装引擎单元测试 | 否 |
| `backend/tests/test_text_pipeline.py` | 文本清洗 4 规则 | 否 |
| `backend/tests/test_role_parse.py` | 角色 JSON 多级容错 | 否 |
| `backend/tests/test_script_service.py` | 分镜编号解析 | 否 |
| `backend/tests/test_image_generation.py` | 出图状态机 | 否 |
| `backend/tests/test_export.py` | 长图拼接 / ZIP | 否 |
| `backend/tests/test_art_style.py` | 画风 CRUD + 绑定 | 否 |

### 7.2 跑测试

```bat
conda activate catong_gen
cd /d D:\github.io\catong_gen\backend

:: 全部
pytest

:: 只跑端到端
pytest -v tests/test_e2e_api.py tests/test_e2e_full_flow.py

:: 跑端到端并打印日志
pytest -v -s tests/test_e2e_api.py
```

### 7.3 一键脚本

```bat
:: scripts\run_e2e_test.bat
:: 1) 启后端（后台）-> 2) 等就绪 -> 3) pytest -> 4) 关后端 -> 5) 汇总
D:\github.io\catong_gen\scripts\run_e2e_test.bat
```

### 7.4 测试隔离

- 端到端测试启动时会建一个**独立测试 DB**（`test_phase5.db` 或类似），不污染主库。
- `httpx.AsyncClient(transport=ASGITransport(app=app))` 直接在内存里跑，不占端口。
- LLM 用 `monkeypatch` 替换 `app.adapters.llm.chat`；生图用 `monkeypatch` 替换 `app.adapters.image_provider.ImageProviderAdapter.generate_image`，落盘到临时目录。

### 7.5 CI 集成示例（GitHub Actions）

```yaml
name: e2e
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: conda-incubator/setup-miniconda@v3
        with: { environment-file: environment.yml, activate-environment: catong_gen }
      - run: |
          cd backend
          python -m app.seeds.seed || true
          pytest -v
```

---

## 8. 接口速查表

> 按调用顺序；详细参数见 Swagger / [`设计文档/07-API接口设计.md`](./07-API接口设计.md)。

| 阶段 | Method | Path | 入参 | 出参关键字段 |
|---|---|---|---|---|
| 健康 | GET | `/api/health` | — | `status` |
| 设置 | GET | `/api/settings` | — | K/V dict |
| 设置 | PUT | `/api/settings` | `{key,value}` | `ok` |
| 供应商 | GET | `/api/provider-accounts` | — | `items[].api_key_tail` |
| 供应商 | POST | `/api/provider-accounts` | `{provider_code,base_url,api_key,model,...}` | `id` |
| 拼装 | GET | `/api/assemble/stages` | — | 8 阶段 dict |
| 拼装 | POST | `/api/assemble/preview` | `{stage,preset_id?,variables,persist_log?}` | `segments,rendered,log_id` |
| 渲染历史 | GET | `/api/render-logs` | `stage?,page,page_size` | `items[]` |
| 渲染历史 | GET | `/api/render-logs/detail` | `log_id` | `rendered,context_json` |
| 提示词 | GET | `/api/prompts` | `category?,purpose?,keyword?` | `items[]` |
| 预设 | GET | `/api/prompt-presets` | `stage?` | `[]` |
| 预设 | POST | `/api/prompt-presets/{id}/activate` | path `id` | `{stage,is_active}` |
| 小说 | POST | `/api/novels` | `{name?,text}` | `{id,name}` |
| 小说 | POST | `/api/novels/import-txt` | form `file` | `{id,name,length}` |
| 小说 | GET | `/api/novels/detail` | `novel_id` | 详情 |
| 小说 | POST | `/api/novels/clean` | `novel_id + {rules,apply}` | `{counts,diff?,is_*_cleaned}` |
| 角色 | POST | `/api/roles/derive` | `{novel_id,preset_id?,provider_code?,replace_existing?}` | `{count,roles,log_id}` |
| 角色 | GET | `/api/roles` | `novel_id?` | `items[]` |
| 角色 | PUT | `/api/roles/tags` | `role_id + tags[]` | `{ok,count}` |
| 项目 | POST | `/api/projects` | `{name,novel_id,mode,...}` | `id` |
| 项目 | POST | `/api/projects/summary` | `project_id + {max_words?}` | `{summary,log_id}` |
| 项目 | POST | `/api/projects/split` | `project_id + {preset_id?,replace_existing?}` | `{count,first,log_id}` |
| 分镜 | GET | `/api/projects/scripts` | `project_id` | `items[]` |
| 分镜 | PUT | `/api/projects/scripts/detail` | `script_id + ScriptIn` | `id`（自动置 `prompt_touched`） |
| 画风 | POST | `/api/art-styles` | `{code,name,prefix,suffix,...}` | `id` |
| 画风 | POST | `/api/art-styles/bind` | `{project_id,style_id}` | `{project_id,style_id}` |
| 图片 prompt | POST | `/api/image-derive/scripts/{script_id}` | `{preset_id?,provider_code?,model?}` | `{script_id,image_prompt,render_log_id}` |
| 图片 prompt | POST | `/api/image-derive/projects/{project_id}/batch` | `{script_ids?,preset_id?,provider_code?,model?}` | `{total,success,failed,results}` |
| 出图 | POST | `/api/image-generation/generate` | `{script_id,provider_code?,model?,aspect_ratio?,resolution?}` | `{task_id,status,image_url}` |
| 出图 | POST | `/api/image-generation/batch` | `{script_ids[],...}` | `{total,success,failed,results}` |
| 出图 | POST | `/api/image-generation/select-main` | `{script_id,task_id}` | `{script_id,main_image}` |
| 出图 | GET | `/api/image-generation/tasks` | `script_id?,status?` | `[]` |
| 出图 | GET | `/api/image-generation/candidates/{script_id}` | path | `[]` |
| 导出 | GET | `/api/export/info/{project_id}` | path | `{can_export,...}` |
| 导出 | POST | `/api/export/create` | `{project_id,export_style,page_numbers,titles,quality}` | `ExportTaskResponse` |
| 导出 | GET | `/api/export/download/{task_id}` | path | FileResponse(jpg) |
| 导出 | POST | `/api/export/zip/{project_id}` | path | FileResponse(zip) |
| 导出 | GET | `/api/export/tasks` | `project_id?` | `[]` |

---

## 附录 A：变量约定

| 占位符 | 含义 | 来源 |
|---|---|---|
| `NOVEL_ID` | 小说主键 | `POST /api/novels` 或 `/api/novels/import-txt` |
| `ROLE_ID_*` | 角色主键 | `GET /api/roles?novel_id=…` |
| `PROJECT_ID` | 项目主键 | `POST /api/projects` |
| `SCRIPT_IDS` | 分镜主键列表 | `GET /api/projects/scripts?project_id=…` |
| `STYLE_ID` | 画风主键 | `POST /api/art-styles` |
| `TASK_ID` | 图片任务主键 | `POST /api/image-generation/generate` |
| `EXPORT_TASK_ID` | 导出任务主键 | `POST /api/export/create` |

## 附录 B：阶段 → 预设 → 模板 对照

| 阶段 (`stage`) | 预设 (`preset_id`) | 模板名 |
|---|---|---|
| `role_derive` | `role_derive_vip` | VIP智能角色推导-v1 |
| `storyboard_short` | `storyboard_short_v1` | 分镜拆分-短文本版-v1 |
| `storyboard_long` | `storyboard_long_v1`（默认 inactive） | 分镜拆分-长文本版-v1 |
| `dialogue_split` | `dialogue_split_v1` | 一键对话分离-v1 |
| `rewrite` | `rewrite_v1` | 文案同义改写-v1 |
| `summary` | `summary_v1` | 项目摘要-v1 |
| `image_derive` | `plot_manga_fusion_bw` | 条漫三段式推导-v1 |
| `role_portrait` | `portrait_s1_v1` | 角色三视图拼装-v1 |
